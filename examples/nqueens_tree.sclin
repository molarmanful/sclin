( init branch free show )#.
8init

#init =$n.
  $n O>a 2^` >A [] 0branch

#branch ( a b m )=.
  $m $n = ( $b 0 $n *` show end ) &#.
  $a ( 0: $m = ) part.
  ,_ \ \; +` tap
    swap 1.: Q $b +`.
    \free dip $m1+ branch

#free.
  \ ( ddup. ) +` fltr
    != &/ rot_ - abs ,_ != &

#show =$x.
  \; tap ""n>o
    $x 1 rot :=.
    ".Q" swap : w>< n>o